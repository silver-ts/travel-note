/**
 * App config constants
 */
const settings = {
  // Entry log input text length
  TITLE_CHAR_LIMIT: 50,
  CONTENT_CHAR_LIMIT: 200,

  // Map marker size in px
  MARKER_SIZE: 32,
};

export default settings;
